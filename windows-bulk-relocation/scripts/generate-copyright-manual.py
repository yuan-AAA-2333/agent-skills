#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""生成两份软著软件说明书的初稿。"""
from __future__ import annotations

import sys
from pathlib import Path

sys.stdout.reconfigure(encoding="utf-8")

COMMON_TAIL = """
---

## 六、运行环境

| 项 | 要求 |
|---|---|
| 操作系统 | Windows 10 / 11（64 位）；亦可在 Linux 下运行 |
| 运行环境 | Python 3.10 及以上 |
| 依赖库 | numpy、pandas、scipy、matplotlib、openpyxl |
| 硬件 | 普通 PC 即可；内存建议 8 GB 以上 |
| 显卡 | 不需要（本系统为纯 CPU 数值计算） |
| 输入 | Excel 算例文件 / YAML 配置；或直接使用内置默认配置 |
| 输出 | PNG 图片、JSON、Excel 表格 |

### 安装与启动

```powershell
# 1) 进入工程目录
cd "<工程目录>"

# 2) 设置模块搜索路径（免安装模式）
$env:PYTHONPATH = "$PWD\\src"

# 3) 查看帮助
python -m <包名>.cli --help

# 4) 跑测试（可选，验证环境）
python -m pytest tests -o addopts="" -q
```

也可以安装为命令（提供 `pyproject.toml`）：

```powershell
python -m pip install -e .
<命令名> --help
```

---

## 七、错误处理与提示

系统对常见问题给出**中文明确提示**，而不是抛裸异常：

| 情况 | 系统行为 |
|---|---|
| 算例文件不存在 | 报错并列出**尝试过的全部路径** |
| Excel 缺少必需工作表 | 报错并说明期望的工作表名与实际名称 |
| 配置项越界或自相矛盾 | `check` 子命令列出全部问题（不静默修正） |
| 缺少可选依赖（如量子 SDK） | **自动降级**到本地算法并说明原因，继续运行 |
| 出图失败 | 打印警告，**求解结果仍然保留并导出** |
| 参数写错 | 命令行参数校验直接拒绝，并给出候选取值 |

---

## 八、技术特点小结

1. **模型与算法分层解耦**——评估内核是唯一的成本定义，各求解模块围绕它对齐。
2. **可选依赖可降级**——缺少商业 SDK 或绘图库时功能自动退化，不中断主流程。
3. **配置驱动**——全部参数外置，换场景/换规模不需要改代码。
4. **结果可复现**——随机种子可配置；求解过程分阶段记录耗时与目标值。
5. **带质量自证**——提供暴力搜索基准，能回答"离最优有多远"。
6. **中文友好**——终端输出、图表标注、报错信息全中文；自动探测可用中文字体。

---

## 九、说明

* 本说明书为**初稿**，用于软件著作权登记申请；正式提交前请按登记机构的最新要求调整格式。
* 文中「开发完成日期」「首次发表日期」请以实际日期填写。
* 截图位置已在文中标注，请在正式稿中替换为实际运行截图。
"""

SHEET_B = """# 烟幕干扰弹投放策略优化系统 V1.0 — 软件说明书

| 项 | 内容 |
|---|---|
| **软件全称** | 烟幕干扰弹投放策略优化系统 |
| **版本号** | V1.0 |
| **著作权人** | 胡声源 |
| **开发完成日期** | 2026 年 6 月 __ 日 |
| **首次发表日期** | 2026 年 6 月 __ 日 |
| **开发语言** | Python |
| **源程序量** | 3,253 行（去空行，含 23 个源文件） |

---

## 一、软件概述

本软件面向**「在运动平台与受保护目标之间投放烟幕、使来袭观察者无法看清目标」**这一类问题，
提供从场景建模、遮蔽效果仿真到投放策略优化的完整工具链。

### 1.1 它解决什么问题

给定一个受保护目标、一个按已知轨迹逼近的观察者、一个可机动的投放平台与一种烟幕遮蔽体，
求投放平台的**飞行方位角、飞行速度、投放时刻、起爆延迟**四个决策量，
使**有效遮蔽总时长最大**。

遮蔽的判定是严格的几何问题：在每一时刻，判断"观察者到目标的视线线段"
是否与"以烟幕云团中心为球心、以有效半径为半径的球体"相交。

### 1.2 主要功能

| 功能 | 说明 |
|---|---|
| 场景装配 | 用配置文件描述任意数量的运动实体（目标 / 观察者 / 投放平台）与遮蔽体 |
| 遮蔽仿真 | 逐时刻判定视线是否被遮蔽，提取有效遮蔽区间与总时长 |
| 策略优化 | 四阶段混合优化搜索最优投放策略 |
| 敏感性分析 | 扫描各决策量，给出影响曲线与关键性排序 |
| 稳健性评估 | 对最优解施加扰动，统计成本分布与"保持 90% 以上"的比例 |
| 结果可视化 | 自动生成六类图表 |
| 批量导出 | 结构化 JSON 与运行产物落盘 |

### 1.3 应用场景

无人机投放干扰弹掩护地面目标、舰艇释放烟幕规避反舰导弹、
地面车辆释放烟幕遮蔽来袭车辆等**同类遮蔽对抗问题**。

---

## 二、运行环境与安装

（见文末「六、运行环境」通用章节）

### 2.1 快速验证

```powershell
$env:PYTHONPATH = "$PWD\\src"
python -m smoke_screen.cli check -c case_missile_drone.yaml    # 校验配置
python -m smoke_screen.cli eval -x 4.1136 140 4.0 7.61          # 评估一组参数
```

---

## 三、功能模块说明

| # | 模块 | 文件 | 职责 |
|---|---|---|---|
| 1 | 配置模块 | `config.py` | 读取 YAML 配置，支持环境变量覆盖；语义校验 |
| 2 | 运动学模块 | `kinematics.py` | 匀加速轨迹、到达时间解析解、最近接近时刻 |
| 3 | 几何模块 | `geometry.py` | 线段-球体相交、线段-圆柱相交（全部向量化） |
| 4 | 场景模块 | `scenario.py` | 把配置装配成可计算的物理模型；求解放置方案 |
| 5 | 仿真模块 | `simulator.py` | 遮蔽判定、区间提取、轨迹生成 |
| 6 | 优化模块 | `optimizer.py` | 四阶段混合优化（含 best-so-far 跟踪） |
| 7 | 敏感性模块 | `sensitivity.py` | 单变量扫描与稳健性评估 |
| 8 | 报告模块 | `reporting.py` | 控制台报告与结构化数据组装 |
| 9 | 可视化模块 | `plotting.py` | 六类结果图 |
| 10 | 命令行模块 | `cli.py` | 子命令解析与流程编排 |

### 3.1 命令一览

| 命令 | 作用 | 退出码 |
|---|---|---|
| `configs` | 列出可用场景配置 | 0 |
| `show` | 打印场景装配信息 | 0 |
| `check` | 校验配置合法性 | 0 通过 / 2 有问题 |
| `eval` | 评估一组给定决策量 | 0 / 2 非法方案 / 3 参数个数错 |
| `optimize` | 执行四阶段优化 | 0 |
| `sweep` | 单变量敏感性扫描 | 0 |
| `report` | 优化 + 敏感性 + 稳健性 + 导出 | 0 |
| `all` | 优化 → 出图 → 导出（一键全流程） | 0 |

---

## 四、操作说明

### 4.1 典型流程

```
① 确认配置 → ② 校验 → ③ 评估已知参数 → ④ 优化 → ⑤ 出图与导出
```

**步骤 1：查看场景装配信息**

```powershell
python -m smoke_screen.cli show -c case_missile_drone.yaml
```

输出内容：受保护目标、观察者（含到达时刻）、投放平台的初始位置与速度、
遮蔽体参数（半径 / 有效时长 / 下沉速度）、四个决策变量的取值范围。

> 【截图位置 1】场景装配信息输出

**步骤 2：校验配置**

```powershell
python -m smoke_screen.cli check -c case_missile_drone.yaml
```

配置有问题时逐条列出（例如时间步长相对仿真时长过大、决策变量上下界颠倒）。

**步骤 3：评估一组给定策略**

```powershell
python -m smoke_screen.cli eval -x 4.1136 140 4.0 7.61 --details
```

四个数依次为：方位角(rad) 速度(m/s) 投放时刻(s) 起爆延迟(s)。
`--details` 额外打印"视线到云团中心的最短距离"诊断。

> 【截图位置 2】策略评估报告（含投放点、起爆点、遮蔽区间）

**步骤 4：执行优化**

```powershell
python -m smoke_screen.cli optimize -c case_missile_drone.yaml
```

系统依次执行四个阶段，并打印每阶段的耗时与结果：

| 阶段 | 作用 |
|---|---|
| 阶段 1 全网格点粗搜 | 以极低成本定位"有遮蔽"的区域，避免从全零平台出发 |
| 阶段 2 差分进化 | 在粗搜解附近开窗做全局搜索 |
| 阶段 3 多起点单纯形 | 局部精化，多次重启避免局部最优 |
| 阶段 4 高精度复核 | 用更细的时间步重算，给出可信的最终时长 |

> 【截图位置 3】四阶段优化过程与最终结果

**步骤 5：一键全流程**

```powershell
python -m smoke_screen.cli all -c case_missile_drone.yaml
```

自动完成"优化 → 出六张图 → 写报告与 JSON"。

> 【截图位置 4】六张结果图总览

### 4.2 输出文件说明

| 文件 | 内容 |
|---|---|
| `result.json` | 完整结果：场景快照、决策量、投放/起爆点、遮蔽区间、各阶段耗时、稳健性指标 |
| `fig1_trajectory_3d.png` | 三维轨迹总览（观察者 / 平台 / 遮蔽体） |
| `fig2_projections.png` | XY 与 XZ 平面投影（含局部放大） |
| `fig3_shield_timeline.png` | 遮蔽时间线 + 高度剖面 + 距离剖面 |
| `fig4_timing.png` | 关键时序关系 |
| `fig5_sensitivity.png` | 决策变量敏感性曲线 |
| `fig6_distance.png` | 视线到遮蔽体中心距离诊断 |

### 4.3 换成同类问题

软件采用**配置驱动**设计：换一道同类遮蔽问题只需复制 `configs/generic_3d.yaml`，
修改 `bodies` / `screen` / `decision` / `time` 四段参数，**不需要修改任何代码**。
软件内置两个场景实例（空中来袭目标、地面来袭车辆）作为参照。

---

## 五、算法原理

### 5.1 遮蔽判定

设观察者位于 O、目标参考点位于 T、遮蔽体中心位于 C。
视线即线段 `P(s) = O + s·(T−O)`，`s ∈ [0,1]`。代入球方程整理得：

```
a·s² + b·s + c = 0
a = d·d,  b = 2(O−C)·d,  c = (O−C)·(O−C) − r²,  d = T − O
```

判别式 `Δ = b² − 4ac ≥ 0` 且任一实根落在 `[0,1]` 内 ⇒ 视线被遮蔽。
全部计算向量化，无 Python 循环。

### 5.2 运动模型

统一采用匀加速直线运动 `p(t) = p₀ + v·t + ½a·t²`，
覆盖匀速直线、平抛、自由落体三种情形。遮蔽体分三段（投放前随平台、投放后自由落体、
起爆后匀速下沉）。观察者以恒定速率迎头飞行时，到达时间有解析解 `‖T − p₀‖ / ‖v‖`。

### 5.3 四阶段混合优化

遮蔽时长关于决策量是**分段常数**函数（判定是二值的），且**可行域极窄**
（球体与视线的相交集合在高维决策空间里是一层薄壳）。单一方法都会失败：
梯度法在平台上走不动、纯随机搜索命中率极低。故采用"粗搜定位 → 全局搜索 → 局部精化 →
高精度复核"的组合策略，并全程跟踪最优解（某阶段结果变差时自动忽略）。
"""

SHEET_A = """# 基于 QUBO 建模与混合局部搜索的物流配送路径优化系统 V1.0 — 软件说明书

| 项 | 内容 |
|---|---|
| **软件全称** | 基于 QUBO 建模与混合局部搜索的物流配送路径优化系统 |
| **版本号** | V1.0 |
| **著作权人** | 胡声源 |
| **开发完成日期** | 2026 年 6 月 __ 日 |
| **首次发表日期** | 2026 年 6 月 __ 日 |
| **开发语言** | Python |
| **源程序量** | 4,969 行（去空行，含 27 个源文件） |

---

## 一、软件概述

本软件面向**物流配送路径优化**问题，把四类常见问题统一到同一套模型与求解框架下，
提供从数据读取、建模、求解到结果可视化与导出的完整工具链。

### 1.1 支持的四类问题

| 问题类型 | 含义 | 典型规模 |
|---|---|---|
| 基础路径问题 | 单车辆、无附加约束 | 15 客户 |
| 带时间窗路径问题 | 单车辆 + 软时间窗（早到等待、迟到惩罚） | 15 客户 |
| 大规模路径问题 | 客户数多，需先做空间分解 | 50 客户 |
| 带容量约束的多车辆问题 | 多车辆 + 容量 + 时间窗 | 50 客户 / 6 车 |

### 1.2 主要功能

| 功能 | 说明 |
|---|---|
| 数据读取 | 从 Excel 读取节点属性与旅行时间矩阵，兼容多种列名 |
| 路径评估 | 统一成本定义：运输时间 + 时间窗惩罚 + 容量违规 |
| QUBO 建模 | 把路径问题转化为二次无约束二值优化模型 |
| 多后端求解 | 量子退火 / 模拟退火 / 贪心三种后端可切换，缺依赖自动降级 |
| 混合局部搜索 | 2-opt、扰动、迭代局部搜索、多起点搜索 |
| 空间分解 | K-means / 层次聚类 + 按容量平衡分簇 |
| 降维分析 | MDS / nMDS 从时间矩阵反推二维坐标，并评估嵌入质量 |
| 敏感性分析 | 扫描惩罚系数、时间窗松紧、QUBO 系数、后端参数 |
| 质量自证 | 小规模下暴力枚举给出真实最优，量化差距 |
| 结果可视化 | 七类图表 |

---

## 二、运行环境与安装

（见文末「六、运行环境」通用章节）

### 2.1 快速验证

```powershell
$env:PYTHONPATH = "$PWD\\src"
python -m qubo_dispatch.cli info            # 查看算例信息
python -m qubo_dispatch.cli backends        # 查看可用求解后端
```

---

## 三、功能模块说明

| # | 模块 | 文件 | 职责 |
|---|---|---|---|
| 1 | 配置模块 | `config.py` | 配置解析与语义校验 |
| 2 | 领域模型 | `domain.py` | 节点 / 算例 / 路线 / 解的定义 |
| 3 | 数据读写 | `dataio.py` | Excel 算例读取（列名别名兼容）与结果导出 |
| 4 | 评估内核 | `evaluate.py` | **全系统唯一的成本定义** |
| 5 | QUBO 建模 | `qubo.py` | 位置指派建模、双重约束惩罚、时间窗线性近似、解码 |
| 6 | 求解后端 | `backends.py` | 量子 / 模拟退火 / 贪心，统一接口 |
| 7 | 局部搜索 | `localsearch.py` | 2-opt、扰动、ILS、多起点 ILS |
| 8 | 聚类分解 | `clustering.py` | K-means / 层次聚类 + 容量平衡 |
| 9 | 降维模块 | `reduction.py` | MDS / nMDS + 嵌入质量评估 |
| 10 | 求解流程 | `optimizer.py` | 四类问题的统一求解流程 |
| 11 | 敏感性 | `sensitivity.py` | 参数扫描与稳健性评估 |
| 12 | 暴力基准 | `brute.py` | 小规模枚举真实最优 |
| 13 | 报告模块 | `reporting.py` | 控制台报告与 JSON / Excel 导出 |
| 14 | 可视化 | `plotting.py` | 七类结果图 |
| 15 | 命令行 | `cli.py` | 9 个子命令 |

### 3.1 命令一览

| 命令 | 作用 |
|---|---|
| `configs` | 列出可用配置 |
| `backends` | 查看求解后端可用性与降级原因 |
| `info` | 打印算例信息（节点数、时间窗统计、容量与最少车辆数） |
| `reduce` | MDS / nMDS 降维并评估嵌入质量 |
| `cluster` | 空间聚类分解 |
| `solve` | 求解（`-p` 指定问题类型，`-b` 指定后端） |
| `sweep` | 参数敏感性扫描 |
| `compare` | 一次跑多类问题并输出对比表 |
| `all` | 求解 → 出图 → 导出 |

---

## 四、操作说明

### 4.1 典型流程

```
① 查看算例 → ② 确认后端 → ③ 求解 → ④ 出图导出 → ⑤（可选）敏感性分析
```

**步骤 1：查看算例信息**

```powershell
python -m qubo_dispatch.cli info
```

输出：节点总数、客户数、时间窗统计（最小/平均/最大宽度）、总需求、车容量、
最少车辆数（容量下界）、旅行时间范围。

> 【截图位置 1】算例信息输出

**步骤 2：查看求解后端**

```powershell
python -m qubo_dispatch.cli backends
```

列出三种后端的可用性。若未安装商业量子 SDK 或未配置凭证，
系统会明确说明并通过 `auto` 模式**自动降级**到本地模拟退火，功能不受影响。

> 【截图位置 2】后端可用性输出

**步骤 3：求解**

```powershell
python -m qubo_dispatch.cli solve -p tsptw               # 15 客户单车辆
python -m qubo_dispatch.cli solve -p cvrptw -n 50        # 50 客户多车辆
```

输出包含：最优目标值及其构成（运输时间 / 时间窗惩罚 / 容量违规）、
路径条数、各求解阶段的目标值与耗时、基线对照（贪心 / 随机），
以及每条路径的客户序列与载货量。

> 【截图位置 3】求解报告（含路径明细）

**步骤 4：一键全流程**

```powershell
python -m qubo_dispatch.cli all -p cvrptw -n 50
```

自动完成"求解 → 出图 → 导出 JSON/Excel"。

> 【截图位置 4】七张结果图总览

**步骤 5：敏感性分析**

```powershell
python -m qubo_dispatch.cli sweep penalty -p tsptw --values 5 20 50 100
```

对指定参数做扫描，输出各取值下的目标值、运输时间、迟到客户数等指标。

### 4.2 输出文件说明

| 文件 | 内容 |
|---|---|
| `solution_<问题>.json` | 完整结构化结果 |
| `solution_<问题>.xlsx` | 配送明细 / 阶段记录 / 基线对照三个工作表 |
| `01_配送路径图.png` | 配送路径图（每车次一色） |
| `02_收敛曲线.png` | 迭代收敛曲线 |
| `03_阶段对比.png` | 各阶段与基线的目标值对比 |
| `04_时间窗甘特图.png` | 时间窗区间与实际到达时刻（晚到标红、早到标蓝） |
| `05_QUBO热力图.png` | QUBO 矩阵热力图与稀疏度 |
| `06_聚类结果.png` | 空间聚类分解结果 |
| `07_敏感性曲线.png` | 参数敏感性曲线 |

---

## 五、算法原理

### 5.1 路径评估

```
t ← 0，当前节点 ← 仓库
对路径中每个后续节点 v：
    arrive = t + w[u][v]
    start  = max(arrive, a_v)          ← 早到需等到时间窗下界
    early  = max(a_v − arrive, 0)      ← 早到量
    late   = max(arrive − b_v, 0)      ← 迟到量
    leave  = start + s_v
    t ← leave
```

目标函数：`minimize  Σ w[u][v] + M1·Σ early + M2·Σ late + 容量违规惩罚`。

### 5.2 QUBO 建模

采用**位置指派**形式：变量 `x[i,k] = 1` 表示客户 i 被安排在第 k 个访问位置，
共 `n²` 个变量。"每个位置恰有一个客户、每个客户恰占一个位置"用二次惩罚表达：
把 `(Σx − 1)²` 展开后，对角系数取 **−P**、同位置不同客户的交叉系数取 **+2P**、
并保留常数 **+P**。

### 5.3 为什么必须"QUBO + 局部搜索"两段式

QUBO 只能表达二次以下的目标，而带时间窗的路径成本是到达时刻的非线性函数。
因此 QUBO 只负责给出**方向性初始解**，真正的成本下降来自局部搜索。
本软件的实测数据也印证了这一点：初始解直接评估为 10077，经多起点迭代局部搜索精修后降到 3910。

### 5.4 大规模分解

`n` 个客户的 QUBO 有 `n²` 个变量（`n=50` 时 2500 个），直接求解不现实。
本软件先按**空间邻近性**聚类（K-means 或层次聚类），
对每簇单独建模求解后拼接，并做**容量平衡**防止某簇总需求超过车容量。

### 5.5 质量自证

元启发式无法自证接近最优，本软件提供暴力枚举基准：
客户数不超过 9 时枚举全部排列算出**真实最优**，据此量化差距。
实测在 7 / 8 / 9 客户上，本软件结果与真实最优的差距均为 **0.00%**。
"""

for name, text in [("B", SHEET_B), ("A", SHEET_A)]:
    pass

targets = {
    "B": Path(r"G:\Code\01-科研\Mathematical-Modeling-Research\期中作业\smoke_screen_opt\docs\软著_软件说明书.md"),
    "A": Path(r"G:\Code\01-科研\Mathematical-Modeling-Research\mathorcup\代码\qubo_dispatch\docs\软著_软件说明书.md"),
}
for key, text in [("B", SHEET_B), ("A", SHEET_A)]:
    p = targets[key]
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(text + COMMON_TAIL, encoding="utf-8", newline="\n")
    print(f"  [{key}] 已写出 {p}（{len(text + COMMON_TAIL)} 字符）")
